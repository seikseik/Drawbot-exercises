canvas = 100
size(canvas,canvas)

raggio=10
oval(canvas/2-raggio,0-raggio,raggio*2,raggio*2)

oval(canvas/2-raggio,canvas-raggio,raggio*2,raggio*2)

rect(0-raggio,canvas/2-raggio,raggio*2,raggio*2)

rect(canvas-raggio,canvas/2-raggio,raggio*2,raggio*2)